## Documentation conventions
- All doc comments in English
- Module-level: `//!` with Purpose, Exports with simple explanation, Key design decisions, Dependencies
- Function-level: `///` only for non-obvious logic + `# Errors` section
- Skip docs on obvious private helpers

## Language
- Code, comments, commits — English only